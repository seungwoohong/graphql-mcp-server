# GraphQL MCP Server Configuration
# Copy this file to .env and update with your actual GraphQL endpoint

# Server Configuration
PORT=8080
NODE_ENV=production

# GraphQL API endpoint URL (for default setup)
GRAPHQL_ENDPOINT=https://api.example.com/graphql

# Optional: Authentication token (if required)
# GRAPHQL_TOKEN=your-auth-token-here

# Note: When using the HTTP server, clients will provide their own
# GraphQL endpoint and token via the /mcp/create endpoint
